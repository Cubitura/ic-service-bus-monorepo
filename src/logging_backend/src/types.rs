use candid::{CandidType, Decode, Encode};
use serde::{Deserialize, Serialize};

/******************************************************/
//
//  GENERAL PURPOSE
//
/******************************************************/
/*
#[derive(CandidType, Deserialize, Serialize)]
pub struct LogEntry {
    pub logId: String,              // Log ID
    pub logType: u8,            // Success, Error, Warning
    pub logOrigin: String,          // Calling service
    pub logCanister: String,        // Service canister ID    
    pub logMessage: String,         // Success/Error message
    pub logData: String,            // Payload consumed by the service
    pub logTimestamp: String,       // Timestamp
}
*/

